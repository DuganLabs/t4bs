# Pins `doppler run` to this repo's project and config, so nobody passes
# --project/--config by hand and nobody deploys against the wrong one.
# One root config, `repository`, keeps the account on Doppler's free tier.
setup:
  project: t4bs
  config: repository
